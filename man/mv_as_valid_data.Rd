% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mv_as_valid_data.r
\name{mv_as_valid_data}
\alias{mv_as_valid_data}
\title{Convert and Validate Data in One Step}
\usage{
mv_as_valid_data(x, ..., .name_repair = "unique")
}
\arguments{
\item{x}{Object to convert (matrix, list, vector, etc.)}

\item{...}{Additional arguments passed to mv_validate_data}

\item{.name_repair}{How to handle column names (uses vctrs::vec_as_names)}
}
\value{
A validated data frame
}
\description{
Convenience function that converts various data formats to a validated
data frame suitable for use in the macroverse ecosystem. Combines
conversion and validation in a single step.
}
\examples{
# Convert matrix
mat <- matrix(1:12, nrow = 3)
df <- mv_as_valid_data(mat)

# Convert list
lst <- list(x = 1:5, y = letters[1:5])
df <- mv_as_valid_data(lst)

# Convert and validate existing data frame
df <- mv_as_valid_data(iris)
}
