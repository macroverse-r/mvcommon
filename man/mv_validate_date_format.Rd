% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mv_validate_date_format.r
\name{mv_validate_date_format}
\alias{mv_validate_date_format}
\title{Validate and Parse Date Formats}
\usage{
mv_validate_date_format(dates, format = "auto", convert = FALSE)
}
\arguments{
\item{dates}{Character vector or Date vector of dates to validate}

\item{format}{Expected format: "year", "quarter", "date", or "auto"}

\item{convert}{Logical, whether to convert to Date objects}
}
\value{
If convert = TRUE, returns Date vector. Otherwise returns
validated character vector in standard format.
}
\description{
Validates date formats and converts them to standard formats used
throughout the macroverse ecosystem. Handles years, quarters, and
other date formats consistently.
}
\examples{
# Validate years
mv_validate_date_format(c("2020", "2021"), format = "year")

# Validate quarters
mv_validate_date_format(c("2020Q1", "2020Q2"), format = "quarter")

# Auto-detect format
mv_validate_date_format(c("2020", "2020Q1"), format = "auto")
}
