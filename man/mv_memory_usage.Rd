% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mv_memory_usage.r
\name{mv_memory_usage}
\alias{mv_memory_usage}
\title{Check Memory Usage of Data Objects}
\usage{
mv_memory_usage(object, operation = NULL, ...)
}
\arguments{
\item{object}{R object to check}

\item{operation}{Optional operation to estimate ("join", "pivot", "aggregate")}

\item{...}{Additional objects for multi-object operations}
}
\value{
Invisibly returns a list with size information. Prints a
formatted summary to the console.
}
\description{
Reports memory usage of data objects and estimates memory requirements
for operations. Useful for preventing out-of-memory errors when working
with large datasets.
}
\examples{
# Check single object
mv_memory_usage(iris)

# Estimate join operation
df1 <- data.frame(id = 1:1000, value = rnorm(1000))
df2 <- data.frame(id = 1:1000, other = rnorm(1000))
mv_memory_usage(df1, operation = "join", df2)
}
